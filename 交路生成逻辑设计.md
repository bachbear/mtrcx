# 地铁交路生成逻辑设计方案

## 1. 总体设计方案
1.1 使用交路票算法，把某一车次按照线路的站点属性(目前仅考虑司务站点，就是司机的工作和出退勤站点)，切分成不同的分段，每一个分段就是一张交路票，代表一次司机在司务站点，上下车和乘坐该车次的行程；
1.2 遍历所有的车次，调用交路票生成算法，生成所有的交路票，形成交路票池；
1.3 一个交路票夹就是根据业务逻辑算法，挑选出来的一组交路票，按照交路票序号升序排列，中间用字符串"->"连接；

## 2. 交路票设计

2.1 使用交路票概念，交路票跟火车票地铁票类似，用以下4个数据字段保持记录的唯一性：车次，接车站点，退车站点，座位类型（司机座位，便乘座位）；用交路票id来作为主键；查询列车时刻表明细表，获得以下两个字段的时间数据：接触站到站时间，退车站到站时间；字段is_sold用来标记该交路票是否已售出；

2.2 交路票生成算法是：目前描述的是座位类型为司机座位的交路票生成算法，后续如果有其他座位类型，需要新增其他的生成算法；
2.2.1 对当天所有的车次按发车时间递增排序；
2.2.2 获得列车车次名称作为交路票的车次，在列车时刻表明细表中，针对该车次，按照到达时间升序遍历每个station_id，如果该站点是该车次的第一个站点，则记录下该station_id对应的车站名称作为接车站点；然后继续遍历后续的station_id，查询逻辑车站表，如果该station_id是司务站点，记录下该station_id对应的车站名称作为退车站点。然后把获得的车次名称，接车站点，退车站点，加上系统自动生成的交路票id，座位类型设置为司机座位，新增这一条记录作为一条交路票记录；
2.2.3 把用2.2.2步骤新增的交路票记录称作A记录，查询A记录里车次名称对应的车次明细表，如果A记录的退车站点是A记录里车次的最后一个站点，则不做处理；如果不是，则把这个退车站点作为步骤2.2.3.1的输入，进行后续处理；
2.2.3.1 新增一条交路票记录，把输入的退车站点作为这条交路票记录的接车站点，然后继续遍历这个接车站点的后续的station_id，查询逻辑车站表，如果该station_id是司务站点，就记录下该station_id对应的车站名称作为这条交路票记录的退车站点；如果所有后续站点都不是司务站点，则把该车次的最后一个站点作为这条交路票记录的退车站点。然后把获得的车次名称，接车站点，退车站点，加上系统自动生成的交路票id，座位类型设置为司机座位，保存这一条交路票记录；
2.2.3.2 如果2.2.3.1步骤生成的交路票里的退车站点不是该交路票车次的最后一个站点，则把这个退车站点作为步骤2.2.3.1的输入，再次进行处理；
2.2.4 重复2.2.2-2.2.3，对所有的车次，生成交路票记录；
2.2.5 对所有的交路票记录，查询列车时刻表明细表，获得接车站到站时间，退车站到站时间，更新到交路票表中；
2.2.6 新生成的交路票数据的is_sold字段设置为false,表示初始状态都是未出售的，后续通过筛选流程，确定这个字段是否变更为true，表示被挑选进入了交路票夹记录中；
2.2.7 对生成的所有交路票按接车站到站时间升序排列，如果接车站到站时间相同，则叠加按车次号升序条件排列，然后赋予唯一的从1开始的交路票序号，每张交路票序号值递增1；

2.3 交路票数据库表设计

### drive_ticket（交路票表）
| 字段名 | 字段类型 | 字段描述 | 是否主键 | 是否可为空 |
|--------|----------|----------|----------|------------|
| id | BIGINT | 主键ID | 是 | 否 |
| ticket_id | BIGINT | 交路票序号 | 否 | 否 |
| train_name | VARCHAR(255) | 车次名称 | 否 | 否 |
| start_station | VARCHAR(255) | 接车站点 | 否 | 否 |
| end_station | VARCHAR(255) | 退车站点 | 否 | 否 |
| seat_type | VARCHAR(255) | 座位类型 | 否 | 否 |
| is_sold | BOOLEAN | 是否已售出 | 否 | 否 |
| pickup_time | TIME | 接车站到站时间 | 否 | 否 |
| dropoff_time | TIME | 退车站到站时间 | 否 | 否 |

## 3. 交路票夹设计

3.1 交路票夹的数据格式为：
### drive_ticket_collect（交路票夹表）
| 字段名 | 字段类型 | 字段描述 | 是否主键 | 是否可为空 |
|--------|----------|----------|----------|------------|
| id | BIGINT | 主键ID | 是 | 否 |
| collect_id | BIGINT | 交路票夹序号 | 否 | 否 |
| ticket_chain | VARCHAR(255) | 交路票序号链 | 否 | 否 |

3.2 定义一个交路票夹生成算法，算法如下：
3.2.1 把交路票数据表中的所有记录的is_sold字段设置为false；
3.2.2 调用"交路票挑选逻辑"，从交路票池中挑选一张交路票作为初始交路票，同时生成一条新的交路票夹记录，赋予交路票夹序号，记录交路票序号到交路票序号链中；
3.2.3 调用"交路票串联逻辑"，挑选下一张交路票；如果无法选出下一张交路票，则更新保存交路票序号链；
3.2.4 调用"交路截止逻辑"，把3.2.2产生的交路票序号链和3.2.3产生的交路票作为输入参数，判断是否需要继续挑选下一张交路票；如果交路截止逻辑返回false,则保存当前交路票序号到交路票序号链中，继续调用交路票串联逻辑；如果交路截止逻辑返回true，或者没有更多的交路票可以挑选，则把交路票序号链中的各个交路票的状态设为已售，退出交路票夹生成算法；
3.2.5 继续调用交路票夹生成算法，生成下一个交路票夹记录；直到所有交路票都被标记为已售状态。

3.3 交路票挑选逻辑：从交路票池中，挑选交路票状态未售，序号最小的交路票；（时间递增原则）

3.4 交路票串联逻辑：从交路票池中挑选出一张交路票的逻辑；
3.4.1 交路票串联逻辑要记录三个状态数据：
3.4.1.1 当前退车站到站时间；
3.4.1.2 当前退车站点名称；
3.4.1.3 当前车次名称；
3.4.2 在当前车次名称为空的时候，挑选交路票状态未售，序号最小的交路票；（时间递增原则）
3.4.3 在当前车次名称不为空的时候，挑选交路票状态未售，相同车次名称，序号最小的交路票；（同一车次原则）
3.4.4 如果运行3.4.3算法，发现没有符合条件的交路票，则运行3.4.5逻辑，找到下一张交路票；
3.4.5 挑选交路票状态未售，接车站到站时间晚于当前退车站到站时间加上变更车次间隔时间，接车站点名称等于当前退车站点名称，序号最小的交路票；（考虑休息时间的相同地点接车原则）
3.4.6 需要维护一个参数，变更车次间隔时间，默认值为30分钟，用来反映司机休息的业务场景；

3.5 交路截止逻辑：用规则检查当前挑选出来的交路票以及当前交路票夹中的交路票链，满足条件，返回true；若不满足，则返回false；
3.5.1 定义交路截止规则，多个规则直接的关系为或；
3.5.1.1 当前交路票的接车站到站时间与当前交路票链的第一张交路票的接车站到站时间间隔超过"最大开车时间"参数；
3.5.1.2 当前交路票的退车站到站时间超过"白班接车时间"参数并且当前交路票链的第一张交路票的接车站到站时间超过"早班退车时间"参数；
3.5.1.3 当前交路票的退车站到站时间超过"夜班接车时间"参数并且当前交路票链的第一张交路票的接车站到站时间超过"白班退车时间"参数；
3.5.1.4 当前交路票的退车站到站时间超过"早班接车时间"参数并且当前交路票链的第一张交路票的接车站到站时间超过"夜班退车时间"参数；
3.5.1.5 当前交路票的接车站等于"强制回程站点"参数；
3.5.2 读取交路参数表中记录，只有is_checked字段的值为true的参数，使用该参数的规则才生效，参与3.5.1的计算；

3.6 交路参数表设计
### drive_ticket_param（交路参数表）
| 字段名 | 字段类型 | 字段描述 | 是否主键 | 是否可为空 |
|--------|----------|----------|----------|------------|
| id | BIGINT | 主键ID | 是 | 否 |
| param_name | VARCHAR(255) | 参数名称 | 否 | 否 |
| param_value | VARCHAR(255) | 参数值 | 否 | 否 |
| param_type | INT | 参数类型(1:时间参数,2:站点参数) | 否 | 否 |
| param_desc | VARCHAR(255) | 参数描述 | 否 | 是 |
| is_checked | BOOLEAN | 是否选中 | 否 | 否 |





